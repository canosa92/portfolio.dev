---
import LinkInline from "./LinkInline.astro"

interface Props {
  title: string
  company: string
  description: string
  link?: string
  date: string
}

const { title, company, description, link, date } = Astro.props
---
<div class="relative mx-8 pb-12">
  <div class="absolute left-0 top-0 h-full w-0.5 bg-gradient-to-b from-yellow-500 to-yellow-200 dark:from-yellow-400 dark:to-yellow-600"></div>
  
  <div class="relative grid gap-8 md:grid-cols-5">
    <div class="md:col-span-2">
      <div class="sticky top-24">
        <div class="relative pl-8">
          <span class="absolute left-0 top-2 size-4 -translate-x-1/2 rounded-full bg-yellow-400 ring-4 ring-white dark:ring-gray-900"></span>
          
          <h3 class="text-xl font-bold text-gray-800 dark:text-gray-100">{title}</h3>
          <h4 class="text-lg font-medium text-gray-600 dark:text-gray-400">{company}</h4>
          <time class="mt-1 block text-sm text-gray-500 dark:text-gray-500">{date}</time>
        </div>
      </div>
    </div>

    <div class="md:col-span-3">
      <div class="prose prose-gray dark:prose-invert">
        <p class="leading-relaxed text-gray-700 dark:text-gray-300">{description}</p>
        {link && (
          <LinkInline 
            href={link}
            class="group inline-flex items-center text-yellow-500 hover:text-yellow-400"
          >
            Saber más
            <svg
              xmlns="http://www.w3.org/2000/svg"
              class="ml-1 size-5 transition-transform group-hover:translate-x-1"
              fill="none"
              viewBox="0 0 24 24"
              stroke="currentColor"
            >
              <path 
                stroke-linecap="round" 
                stroke-linejoin="round" 
                stroke-width="2" 
                d="M9 5l7 7-7 7"
              />
            </svg>
          </LinkInline>
        )}
      </div>
    </div>
  </div>
</div>
